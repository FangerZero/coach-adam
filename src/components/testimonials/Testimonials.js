import './Testimonials.css';
// Testimonial will be created and pass a parameter. 

function Testimonials() {

    return(
        <div id="testimonials" className="Testimonials">
            <div>
                Testimonial 1
                Photo, Name, Age (Optional)
            </div>
            <div>
                Testimonial 2
                Photo, Name, Age (Optional)
            </div>
        </div>
    );
}

export default Testimonials